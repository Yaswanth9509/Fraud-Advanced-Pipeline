# Secrets — never commit real credentials
.streamlit/secrets.toml

# Large raw data — not needed in the repo, only locally for training
data/fraudTrain.csv
data/fraudTest.csv

# Python
__pycache__/
*.pyc
.venv/
.venv312/

# Local-only artifacts (SQLite is no longer used, but harmless to ignore if present)
transactions.db
